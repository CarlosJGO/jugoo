"""Shared visual constants mirrored by shell/style.css foundation tokens."""

from __future__ import annotations

# Inner spacing between elements inside a module container.
SHELL_MODULE_INNER_SPACING = 6

# Spacing between stacked labels in compact vertical modules (e.g. clock).
SHELL_MODULE_STACK_SPACING = 0

# Main bar shell: outer capsule padding is CSS-driven; these are GTK layout gaps.
SHELL_BAR_REGION_SPACING = 8
SHELL_BAR_MODULE_SPACING = 8
